name: Keep Alive

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: |
          curl -s -o /dev/null -w "%{http_code}"             https://loose-number-extractor-zhxnc56fdg7citjbuxaatl.streamlit.app/
